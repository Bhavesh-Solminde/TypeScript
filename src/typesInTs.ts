//Inference
let coffee = "black"; // after this its type can only be string
let caffine = Math.random() * 10 > 5 ? 10 : "5"; // after this the type can either be number or string

//Annotation
let coffeeFlavor: string = "bitter";
let coffeeOrder: number = 5;
let likedCoffee: boolean = true;
